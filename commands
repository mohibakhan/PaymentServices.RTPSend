git rm -r --cached bin obj .vs .vscode .idea
git rm --cached local.settings.json
git rm --cached src/bin -r
git rm --cached src/obj -r
git rm --cached src/local.settings.json

git rm -r --cached .
git add .
