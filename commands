cd C:\path\to\PaymentServices.RTPSend

rem -- init the repo
git init -b main

rem -- configure who you are (skip if already global)
git config user.name "Your Name"
git config user.email "your.email@company.com"

rem -- stage everything
git add .

rem -- first commit
git commit -m "Initial commit: PaymentServices.RTPSend"

rem -- point at ADO
git remote add origin https://dev.azure.com/{org}/{project}/_git/PaymentServices.RTPSend

rem -- push and set upstream
git push -u origin main
