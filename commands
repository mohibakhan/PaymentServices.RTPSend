catch (CosmosException cex)
{
    _logger.LogError(
        "CosmosException: StatusCode={Status} SubStatusCode={SubStatus} ActivityId={Activity} Message={Message}",
        cex.StatusCode, cex.SubStatusCode, cex.ActivityId, cex.Message);
    throw;
}
